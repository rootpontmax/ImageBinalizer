#include "BradleyFilter.h"

////////////////////////////////////////////////////////////////////////////////////////////////////
void CBradleyFilter::ProcessGreyscale( uint8_t *pBuffer, const int sizeX, const int sizeY ) const
{
    CalcIntegralSum( pBuffer, sizeX, sizeY );
}
////////////////////////////////////////////////////////////////////////////////////////////////////
void CBradleyFilter::CalcIntegralSum( uint8_t *pBuffer, const int sizeX, const int sizeY ) const
{
    for( int y = 0; y < sizeY; ++y )
        for( int x = 0; x < sizeX; ++x )
        {
            
        }
}
////////////////////////////////////////////////////////////////////////////////////////////////////
